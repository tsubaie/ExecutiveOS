import type { ClientManifest } from '@/core/modules/manifest';
// The centre is a popover in the shell header, not a page: it declares no nav entry.
export const manifest: ClientManifest = { id: 'notifications' };
