import { AccountPage } from '@/modules/account/ui';
import { PageBody } from '@/ui/layout/PageTransition';
export default function Page() {
  return (
    <PageBody>
      <AccountPage />
    </PageBody>
  );
}
